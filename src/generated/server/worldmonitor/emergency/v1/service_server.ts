// @ts-nocheck
// Code generated by protoc-gen-ts-server. DO NOT EDIT.
// source: worldmonitor/emergency/v1/service.proto

export interface Alert {
  id: string;
  event: string;
  severity: string;
  urgency: string;
  headline: string;
  description: string;
  instruction: string;
  areaDesc: string;
  onset: string;
  expires: string;
  polygon: string;
}

export interface ListActiveAlertsRequest {
  island: string;
}

export interface ListActiveAlertsResponse {
  alerts: Alert[];
}

export interface StreamGauge {
  siteCode: string;
  siteName: string;
  latitude: number;
  longitude: number;
  gaugeHeight: number;
  discharge: number;
  dateTime: string;
  floodStage: number;
}

export interface ListStreamGaugesRequest {
  island: string;
}

export interface ListStreamGaugesResponse {
  gauges: StreamGauge[];
}

export interface Shelter {
  id: string;
  name: string;
  address: string;
  latitude: number;
  longitude: number;
  island: string;
  type: string;
  status: string;
  capacity: number;
  petsAllowed: boolean;
  adaAccessible: boolean;
}

export interface ListSheltersRequest {
  island: string;
}

export interface ListSheltersResponse {
  shelters: Shelter[];
}

export interface RoadClosure {
  id: string;
  road: string;
  location: string;
  reason: string;
  status: string;
  island: string;
  latitude: number;
  longitude: number;
  detour: string;
  updatedAt: string;
}

export interface ListRoadClosuresRequest {
  island: string;
}

export interface ListRoadClosuresResponse {
  closures: RoadClosure[];
}

export interface PowerOutage {
  id: string;
  area: string;
  island: string;
  customersAffected: number;
  cause: string;
  estimatedRestoration: string;
  startTime: string;
}

export interface ListPowerOutagesRequest {
  island: string;
}

export interface ListPowerOutagesResponse {
  outages: PowerOutage[];
}

export interface TideGauge {
  stationId: string;
  stationName: string;
  latitude: number;
  longitude: number;
  waterLevel: number;
  predictedLevel: number;
  timestamp: string;
  island: string;
}

export interface ListTideGaugesRequest {
  island: string;
}

export interface ListTideGaugesResponse {
  gauges: TideGauge[];
}

export interface FieldViolation {
  field: string;
  description: string;
}

export class ValidationError extends Error {
  violations: FieldViolation[];

  constructor(violations: FieldViolation[]) {
    super("Validation failed");
    this.name = "ValidationError";
    this.violations = violations;
  }
}

export class ApiError extends Error {
  statusCode: number;
  body: string;

  constructor(statusCode: number, message: string, body: string) {
    super(message);
    this.name = "ApiError";
    this.statusCode = statusCode;
    this.body = body;
  }
}

export interface ServerContext {
  request: Request;
  pathParams: Record<string, string>;
  headers: Record<string, string>;
}

export interface ServerOptions {
  onError?: (error: unknown, req: Request) => Response | Promise<Response>;
  validateRequest?: (methodName: string, body: unknown) => FieldViolation[] | undefined;
}

export interface RouteDescriptor {
  method: string;
  path: string;
  handler: (req: Request) => Promise<Response>;
}

export interface EmergencyServiceHandler {
  listActiveAlerts(ctx: ServerContext, req: ListActiveAlertsRequest): Promise<ListActiveAlertsResponse>;
  listStreamGauges(ctx: ServerContext, req: ListStreamGaugesRequest): Promise<ListStreamGaugesResponse>;
  listShelters(ctx: ServerContext, req: ListSheltersRequest): Promise<ListSheltersResponse>;
  listRoadClosures(ctx: ServerContext, req: ListRoadClosuresRequest): Promise<ListRoadClosuresResponse>;
  listPowerOutages(ctx: ServerContext, req: ListPowerOutagesRequest): Promise<ListPowerOutagesResponse>;
  listTideGauges(ctx: ServerContext, req: ListTideGaugesRequest): Promise<ListTideGaugesResponse>;
}

export function createEmergencyServiceRoutes(
  handler: EmergencyServiceHandler,
  options?: ServerOptions,
): RouteDescriptor[] {
  return [
    {
      method: "GET",
      path: "/api/emergency/v1/list-active-alerts",
      handler: async (req: Request): Promise<Response> => {
        try {
          const pathParams: Record<string, string> = {};
          const url = new URL(req.url, "http://localhost");
          const params = url.searchParams;
          const body: ListActiveAlertsRequest = {
            island: params.get("island") ?? "",
          };
          if (options?.validateRequest) {
            const bodyViolations = options.validateRequest("listActiveAlerts", body);
            if (bodyViolations) {
              throw new ValidationError(bodyViolations);
            }
          }

          const ctx: ServerContext = {
            request: req,
            pathParams,
            headers: Object.fromEntries(req.headers.entries()),
          };

          const result = await handler.listActiveAlerts(ctx, body);
          return new Response(JSON.stringify(result as ListActiveAlertsResponse), {
            status: 200,
            headers: { "Content-Type": "application/json" },
          });
        } catch (err: unknown) {
          if (err instanceof ValidationError) {
            return new Response(JSON.stringify({ violations: err.violations }), {
              status: 400,
              headers: { "Content-Type": "application/json" },
            });
          }
          if (options?.onError) {
            return options.onError(err, req);
          }
          const message = err instanceof Error ? err.message : String(err);
          return new Response(JSON.stringify({ message }), {
            status: 500,
            headers: { "Content-Type": "application/json" },
          });
        }
      },
    },
    {
      method: "GET",
      path: "/api/emergency/v1/list-stream-gauges",
      handler: async (req: Request): Promise<Response> => {
        try {
          const pathParams: Record<string, string> = {};
          const url = new URL(req.url, "http://localhost");
          const params = url.searchParams;
          const body: ListStreamGaugesRequest = {
            island: params.get("island") ?? "",
          };
          if (options?.validateRequest) {
            const bodyViolations = options.validateRequest("listStreamGauges", body);
            if (bodyViolations) {
              throw new ValidationError(bodyViolations);
            }
          }

          const ctx: ServerContext = {
            request: req,
            pathParams,
            headers: Object.fromEntries(req.headers.entries()),
          };

          const result = await handler.listStreamGauges(ctx, body);
          return new Response(JSON.stringify(result as ListStreamGaugesResponse), {
            status: 200,
            headers: { "Content-Type": "application/json" },
          });
        } catch (err: unknown) {
          if (err instanceof ValidationError) {
            return new Response(JSON.stringify({ violations: err.violations }), {
              status: 400,
              headers: { "Content-Type": "application/json" },
            });
          }
          if (options?.onError) {
            return options.onError(err, req);
          }
          const message = err instanceof Error ? err.message : String(err);
          return new Response(JSON.stringify({ message }), {
            status: 500,
            headers: { "Content-Type": "application/json" },
          });
        }
      },
    },
    {
      method: "GET",
      path: "/api/emergency/v1/list-shelters",
      handler: async (req: Request): Promise<Response> => {
        try {
          const pathParams: Record<string, string> = {};
          const url = new URL(req.url, "http://localhost");
          const params = url.searchParams;
          const body: ListSheltersRequest = {
            island: params.get("island") ?? "",
          };
          if (options?.validateRequest) {
            const bodyViolations = options.validateRequest("listShelters", body);
            if (bodyViolations) {
              throw new ValidationError(bodyViolations);
            }
          }

          const ctx: ServerContext = {
            request: req,
            pathParams,
            headers: Object.fromEntries(req.headers.entries()),
          };

          const result = await handler.listShelters(ctx, body);
          return new Response(JSON.stringify(result as ListSheltersResponse), {
            status: 200,
            headers: { "Content-Type": "application/json" },
          });
        } catch (err: unknown) {
          if (err instanceof ValidationError) {
            return new Response(JSON.stringify({ violations: err.violations }), {
              status: 400,
              headers: { "Content-Type": "application/json" },
            });
          }
          if (options?.onError) {
            return options.onError(err, req);
          }
          const message = err instanceof Error ? err.message : String(err);
          return new Response(JSON.stringify({ message }), {
            status: 500,
            headers: { "Content-Type": "application/json" },
          });
        }
      },
    },
    {
      method: "GET",
      path: "/api/emergency/v1/list-road-closures",
      handler: async (req: Request): Promise<Response> => {
        try {
          const pathParams: Record<string, string> = {};
          const url = new URL(req.url, "http://localhost");
          const params = url.searchParams;
          const body: ListRoadClosuresRequest = {
            island: params.get("island") ?? "",
          };
          if (options?.validateRequest) {
            const bodyViolations = options.validateRequest("listRoadClosures", body);
            if (bodyViolations) {
              throw new ValidationError(bodyViolations);
            }
          }

          const ctx: ServerContext = {
            request: req,
            pathParams,
            headers: Object.fromEntries(req.headers.entries()),
          };

          const result = await handler.listRoadClosures(ctx, body);
          return new Response(JSON.stringify(result as ListRoadClosuresResponse), {
            status: 200,
            headers: { "Content-Type": "application/json" },
          });
        } catch (err: unknown) {
          if (err instanceof ValidationError) {
            return new Response(JSON.stringify({ violations: err.violations }), {
              status: 400,
              headers: { "Content-Type": "application/json" },
            });
          }
          if (options?.onError) {
            return options.onError(err, req);
          }
          const message = err instanceof Error ? err.message : String(err);
          return new Response(JSON.stringify({ message }), {
            status: 500,
            headers: { "Content-Type": "application/json" },
          });
        }
      },
    },
    {
      method: "GET",
      path: "/api/emergency/v1/list-power-outages",
      handler: async (req: Request): Promise<Response> => {
        try {
          const pathParams: Record<string, string> = {};
          const url = new URL(req.url, "http://localhost");
          const params = url.searchParams;
          const body: ListPowerOutagesRequest = {
            island: params.get("island") ?? "",
          };
          if (options?.validateRequest) {
            const bodyViolations = options.validateRequest("listPowerOutages", body);
            if (bodyViolations) {
              throw new ValidationError(bodyViolations);
            }
          }

          const ctx: ServerContext = {
            request: req,
            pathParams,
            headers: Object.fromEntries(req.headers.entries()),
          };

          const result = await handler.listPowerOutages(ctx, body);
          return new Response(JSON.stringify(result as ListPowerOutagesResponse), {
            status: 200,
            headers: { "Content-Type": "application/json" },
          });
        } catch (err: unknown) {
          if (err instanceof ValidationError) {
            return new Response(JSON.stringify({ violations: err.violations }), {
              status: 400,
              headers: { "Content-Type": "application/json" },
            });
          }
          if (options?.onError) {
            return options.onError(err, req);
          }
          const message = err instanceof Error ? err.message : String(err);
          return new Response(JSON.stringify({ message }), {
            status: 500,
            headers: { "Content-Type": "application/json" },
          });
        }
      },
    },
    {
      method: "GET",
      path: "/api/emergency/v1/list-tide-gauges",
      handler: async (req: Request): Promise<Response> => {
        try {
          const pathParams: Record<string, string> = {};
          const url = new URL(req.url, "http://localhost");
          const params = url.searchParams;
          const body: ListTideGaugesRequest = {
            island: params.get("island") ?? "",
          };
          if (options?.validateRequest) {
            const bodyViolations = options.validateRequest("listTideGauges", body);
            if (bodyViolations) {
              throw new ValidationError(bodyViolations);
            }
          }

          const ctx: ServerContext = {
            request: req,
            pathParams,
            headers: Object.fromEntries(req.headers.entries()),
          };

          const result = await handler.listTideGauges(ctx, body);
          return new Response(JSON.stringify(result as ListTideGaugesResponse), {
            status: 200,
            headers: { "Content-Type": "application/json" },
          });
        } catch (err: unknown) {
          if (err instanceof ValidationError) {
            return new Response(JSON.stringify({ violations: err.violations }), {
              status: 400,
              headers: { "Content-Type": "application/json" },
            });
          }
          if (options?.onError) {
            return options.onError(err, req);
          }
          const message = err instanceof Error ? err.message : String(err);
          return new Response(JSON.stringify({ message }), {
            status: 500,
            headers: { "Content-Type": "application/json" },
          });
        }
      },
    },
  ];
}
